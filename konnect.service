[Unit]
Description=Konnect
After=network.target

[Service]
User=user
Restart=always
Type=simple
WorkingDirectory=/home/user/konnect
ExecStart=/usr/bin/pipenv run python konnectd.py --receiver --service

[Install]
WantedBy=multi-user.target
